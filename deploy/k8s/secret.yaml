# =============================================================================
# HIVE-R Kubernetes Secret
# =============================================================================
# Base64-encode your secrets before adding them here:
#   echo -n "your-secret-value" | base64
#
# IMPORTANT: Enable encryption at rest in production:
#   https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: hive-r-secrets
  namespace: default
  labels:
    app: hive-r
    component: secrets
type: Opaque
data:
  # Replace these with your base64-encoded values
  openai_api_key: REPLACE_WITH_BASE64_VALUE
  hive_api_key: REPLACE_WITH_BASE64_VALUE
  jwt_secret: REPLACE_WITH_BASE64_VALUE
  # Optional secrets — remove if not used
  # stripe_secret_key: REPLACE_WITH_BASE64_VALUE
  # stripe_webhook_secret: REPLACE_WITH_BASE64_VALUE
  # sentry_dsn: REPLACE_WITH_BASE64_VALUE
  # langsmith_api_key: REPLACE_WITH_BASE64_VALUE
  # github_token: REPLACE_WITH_BASE64_VALUE
